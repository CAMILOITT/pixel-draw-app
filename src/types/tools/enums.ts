/**
 * Enumeration of drawing tools.
 * @enum {string} list of tools Drawing
 * @prop {string} brush
 * @prop {string} eraser
 * @prop {string} eyeDropper
 * @readonly
 */

export enum Tools {
  brush = 'brush',
  eraser = 'eraser',
  eyeDropper = 'eyeDropper',
}
