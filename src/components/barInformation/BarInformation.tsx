import css from './BarInformation.module.css'

interface BarInformationProps {}

export default function BarInformation({}: BarInformationProps) {
  return <div className={css.barInformation}>position mouse</div>
}
